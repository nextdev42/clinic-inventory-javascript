<!DOCTYPE html>
<html lang="sw">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ongeza Stock ya Dawa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .medicine-card {
            transition: transform 0.2s;
        }
        .medicine-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="text-center mb-5">
            <h1 class="text-primary">📦 Ongeza Stock ya Dawa</h1>
            <p class="lead">Chagua dawa na ongeza kiasi cha stock</p>
        </div>
        
        <% if (success) { %>
            <div class="alert alert-success alert-dismissible fade show">
                ✔️ Stock imeongezwa kikamilifu!
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>
        
        <div class="card shadow">
            <div class="card-body">
                <form method="POST" class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Chagua Dawa</label>
                        <select name="dawaId" class="form-select form-select-lg" required>
                            <option value="">Chagua Dawa...</option>
                            <% dawa.forEach(medicine => { %>
                                <option value="<%= medicine.id %>">
                                    <%= medicine.jina %> 
                                    (Stock: <%= medicine.kiasi || 0 %>)
                                    <% if (medicine.aina) { %>- <%= medicine.aina %><% } %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                    
                    <div class="col-md-6">
                        <label class="form-label fw-bold">Kiasi cha Kuongeza</label>
                        <input type="number" name="kiasi" class="form-control form-control-lg" 
                               min="1" step="1" placeholder="Ingiza kiasi" required>
                    </div>
                    
                    <div class="col-12 mt-4">
                        <button type="submit" class="btn btn-primary btn-lg w-100 py-3">
                            ➕ Ongeza Stock
                        </button>
                    </div>
                </form>
            </div>
        </div>
        
        <div class="mt-5">
            <h3 class="border-bottom pb-2">Dawa Zote</h3>
            <div class="row row-cols-1 row-cols-md-3 g-4 mt-2">
                <% dawa.forEach(medicine => { %>
                    <div class="col">
                        <div class="card medicine-card h-100">
                            <div class="card-body">
                                <h5 class="card-title"><%= medicine.jina %></h5>
                                <% if (medicine.aina) { %>
                                    <p class="card-text text-muted"><%= medicine.aina %></p>
                                <% } %>
                                <p class="display-6"><%= medicine.kiasi || 0 %> <span class="fs-6">units</span></p>
                                <small class="text-muted">
                                    Imesasishwa: <%= medicine.UPDATED_AT ? 
                                        new Date(medicine.UPDATED_AT).toLocaleDateString('sw-TZ') : 
                                        'Tarehe haijulikani' %>
                                </small>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
        
        <div class="text-center mt-5">
            <a href="/" class="btn btn-outline-secondary btn-lg">
                ← Rudi Nyumbani
            </a>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
