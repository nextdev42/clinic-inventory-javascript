<!DOCTYPE html>
<html lang="sw">
<head>
  <meta charset="UTF-8" />
  <title>Maelezo ya Watumiaji</title>
  <style>
    body { font-family: sans-serif; padding: 20px; background: #f3f3f3; }
    table { width: 100%; border-collapse: collapse; background: white; }
    th, td { padding: 10px; border: 1px solid #ccc; }
    th { background: #eef; }
    td.maelezo { font-style: italic; color: #444; }
  </style>
</head>
<body>
  <h2>📋 Ukaguzi wa Maelezo ya Watumiaji</h2>
  <table>
    <tr><th>Jina</th><th>Maelezo</th><th>Urefu</th></tr>
    <% dump.forEach(u => { %>
      <tr>
        <td><%= u.jina %></td>
        <td class="maelezo"><%= u.maelezo %></td>
        <td><%= u.length %></td>
      </tr>
    <% }) %>
  </table>
</body>
</html>
