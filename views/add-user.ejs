<!DOCTYPE html>
<html lang="sw">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ongeza Mtumiaji</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="card shadow">
            <div class="card-header bg-primary text-white">
                <h5 class="card-title mb-0">Ongeza Mtumiaji Mpya</h5>
            </div>
            
            <% if (error) { %>
                <div class="alert alert-danger m-3">
                    <%= error %>
                </div>
            <% } %>
            
            <div class="card-body">
                <form method="POST">
                    <div class="mb-3">
                        <label class="form-label">Jina Kamili</label>
                        <input type="text" name="jina" class="form-control" 
                               value="<%= formData.jina || '' %>" required>
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">Maelezo (Nafasi ya Kazi)</label>
                        <textarea name="description" class="form-control"><%= formData.description || '' %></textarea>
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">Chagua Kliniki</label>
                        <select name="clinicId" class="form-select" required>
                            <option value="">Chagua Kliniki...</option>
                            <% clinics.forEach(clinic => { %>
                                <option value="<%= clinic.id %>" 
                                    <%= (formData.clinicId === clinic.id) ? 'selected' : '' %>>
                                    <%= clinic.jina %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                    
                    <button type="submit" class="btn btn-primary w-100">
                        Hifadhi Mtumiaji
                    </button>
                </form>
            </div>
            
            <div class="card-footer text-center">
                <a href="/" class="btn btn-outline-secondary">
                    ← Rudi Nyumbani
                </a>
            </div>
        </div>
    </div>
</body>
</html>
