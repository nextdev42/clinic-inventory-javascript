<!DOCTYPE html>
<html lang="sw">
<head>
  <meta charset="UTF-8">
  <title>Ongeza Mtumiaji</title>
  <style>
    body {
      font-family: sans-serif;
      background: #f9f9f9;
      padding: 20px;
    }
    h2 {
      text-align: center;
      margin-bottom: 10px;
    }
    form {
      background: white;
      padding: 20px;
      border-radius: 8px;
      max-width: 400px;
      margin: auto;
      box-shadow: 0 0 5px rgba(0,0,0,0.1);
    }
    label {
      display: block;
      margin-top: 15px;
      font-size: 16px;
    }
    input {
      width: 100%;
      padding: 12px;
      margin-top: 5px;
      font-size: 16px;
    }
    button {
      margin-top: 20px;
      padding: 12px;
      background: green;
      color: white;
      border: none;
      cursor: pointer;
      font-size: 16px;
      width: 100%;
      border-radius: 6px;
    }
    .error {
      color: red;
      font-size: 14px;
      margin-top: 5px;
    }
    .success-message {
      display: none;
      text-align: center;
      padding: 15px;
      margin-top: 20px;
      background-color: #e0ffe0;
      color: green;
      font-weight: bold;
      border-radius: 6px;
      animation: fadeIn 1s ease-in forwards;
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to   { opacity: 1; }
    }
    a {
      display: block;
      margin-top: 20px;
      text-align: center;
      font-weight: bold;
      color: #007acc;
      text-decoration: none;
    }
  </style>
</head>
<body>

  <h2>👤 Ongeza Mtumiaji wa Dawa</h2>

  <form id="form" action="/mtumiaji/ongeza" method="POST" onsubmit="return validateForm()">
    <label for="jina">Jina la Mtumiaji:</label>
    <input type="text" id="jina" name="jina" required>
    <div id="jinaError" class="error"></div>

    <button type="submit">💾 Hifadhi</button>
  </form>

  <div id="success" class="success-message">✅ Taarifa imehifadhiwa vizuri!</div>

  <a href="/">⬅️ Rudi kwenye Dashibodi</a>

  <script>
    function validateForm() {
      const jina = document.getElementById('jina').value;
      const errorDiv = document.getElementById('jinaError');
      errorDiv.textContent = '';

      if (!jina || jina.trim().length < 2) {
        errorDiv.textContent = 'Tafadhali jaza jina sahihi (angalau herufi 2).';
        return false;
      }

      document.getElementById('success').style.display = 'block';
      setTimeout(() => {
        document.getElementById('success').style.display = 'none';
      }, 3000);

      return true;
    }
  </script>

</body>
</html>
