<!DOCTYPE html>
<html lang="sw">
<head>
  <meta charset="UTF-8" />
  <title>Ripoti ya Matumizi ya Dawa</title>
  <style>
    body {
      font-family: sans-serif;
      max-width: 900px;
      margin: auto;
      padding: 20px;
      background: #f5f5f5;
    }
    h1 {
      text-align: center;
      margin-bottom: 30px;
    }
    .user-block {
      background: white;
      padding: 15px 20px;
      margin-bottom: 25px;
      border-radius: 8px;
      box-shadow: 0 0 5px rgba(0,0,0,0.1);
    }
    .user-block h2 {
      margin-bottom: 15px;
      color: #007acc;
    }
    .date-group {
      margin-bottom: 15px;
    }
    .date-group h3 {
      background: #007acc;
      color: white;
      padding: 5px 10px;
      border-radius: 5px;
      margin-bottom: 8px;
      font-weight: normal;
    }
    ul {
      list-style: none;
      padding-left: 15px;
    }
    li {
      padding: 4px 0;
    }
    a.back-link {
      display: block;
      margin: 30px auto 10px;
      text-align: center;
      text-decoration: none;
      color: #007acc;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1>📋 Ripoti ya Matumizi ya Dawa kwa Watumiaji</h1>

  <% if(report.length === 0) { %>
    <p>Hakuna taarifa za matumizi.</p>
  <% } %>

  <% report.forEach(user => { %>
    <div class="user-block">
      <h2>👤 <%= user.jina %></h2>

      <% Object.keys(user.matumiziByDate).sort((a,b) => new Date(b) - new Date(a)).forEach(date => { %>
        <div class="date-group">
          <h3><%= new Date(date).toLocaleDateString('sw-TZ', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></h3>
          <ul>
            <% user.matumiziByDate[date].forEach(item => { %>
              <li>
                - <strong><%= item.dawa %></strong>: <%= item.kiasi %> &nbsp;
                <small>(
                  <%= new Date(item.saa).toLocaleTimeString('sw-TZ', { hour: '2-digit', minute: '2-digit' }) %>
                )</small>
              </li>
            <% }) %>
          </ul>
        </div>
      <% }) %>
    </div>
  <% }) %>

  <a href="/" class="back-link">⬅️ Rudi kwenye Dashibodi</a>
</body>
</html>
