<!DOCTYPE html>
<html lang="sw">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Usimamizi wa Watumiaji</title>
  <link href="/fontawesome/css/all.min.css" rel="stylesheet">
  <style>
    :root {
      --primary: #2563eb;
      --primary-hover: #1d4ed8;
      --primary-light: #dbeafe;
      --danger: #dc2626;
      --success: #16a34a;
      --warning: #d97706;
      --teal: #0d9488;
      --purple: #7e22ce;
      --gray-50: #f9fafb;
      --gray-100: #f3f4f6;
      --gray-200: #e5e7eb;
      --gray-300: #d1d5db;
      --gray-500: #6b7280;
      --gray-600: #4b5563;
      --gray-800: #1f2937;
      --white: #ffffff;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background-color: var(--gray-50);
      color: var(--gray-800);
      line-height: 1.5;
    }

    .container {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    /* Header */
    header {
      background-color: var(--primary);
      color: var(--white);
      padding: 1rem 0;
      margin-bottom: 1.5rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header-title {
      font-size: 1.5rem;
      font-weight: 700;
      display: flex;
      align-items: center;
    }

    .header-title i {
      margin-right: 0.75rem;
    }

    /* Stats Cards */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1rem;
      margin-bottom: 1.5rem;
    }

    .stat-card {
      background-color: var(--white);
      border-radius: 0.75rem;
      padding: 1.5rem 1.25rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .stat-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
    }

    .stat-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 5px;
      height: 100%;
    }

    .stat-card:nth-child(1)::before { background-color: var(--primary); }
    .stat-card:nth-child(2)::before { background-color: var(--success); }
    .stat-card:nth-child(3)::before { background-color: var(--danger); }
    .stat-card:nth-child(4)::before { background-color: var(--teal); }

    .stat-card p {
      color: var(--gray-500);
      font-size: 0.875rem;
      margin-bottom: 0.5rem;
    }

    .stat-card h3 {
      font-size: 1.8rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }

    .stat-card .stat-meta {
      font-size: 0.75rem;
      color: var(--gray-500);
      display: flex;
      align-items: center;
    }

    .stat-card .stat-icon {
      position: absolute;
      right: 1.25rem;
      top: 1.25rem;
      font-size: 1.8rem;
      opacity: 0.2;
    }

    /* Search Section */
    .search-section {
      background-color: var(--white);
      border-radius: 0.75rem;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    }

    .search-header {
      display: flex;
      align-items: center;
      margin-bottom: 1.5rem;
      padding-bottom: 0.75rem;
      border-bottom: 2px solid var(--gray-100);
    }

    .search-header i {
      font-size: 1.5rem;
      color: var(--primary);
      margin-right: 0.75rem;
    }

    .search-header h2 {
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--gray-800);
    }

    .search-form {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .search-group {
      flex: 1;
      min-width: 250px;
    }

    .search-label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 500;
      color: var(--gray-700);
    }

    .search-input, .search-select {
      width: 100%;
      padding: 0.75rem 1rem;
      border: 1px solid var(--gray-300);
      border-radius: 0.5rem;
      background-color: var(--white);
      font-size: 1rem;
      transition: all 0.3s;
    }

    .search-input:focus, .search-select:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
      outline: none;
    }

    .search-actions {
      display: flex;
      gap: 0.75rem;
      margin-top: 1.5rem;
    }

    .btn {
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      font-weight: 500;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      transition: all 0.2s;
      font-size: 1rem;
      border: none;
    }

    .btn:hover {
      transform: translateY(-2px);
    }

    .btn-primary {
      background-color: var(--primary);
      color: white;
    }

    .btn-secondary {
      background-color: var(--gray-200);
      color: var(--gray-800);
    }

    /* User Search Card */
    .user-search-card {
      background: linear-gradient(135deg, var(--primary), var(--purple));
      border-radius: 0.75rem;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
      color: white;
    }

    .user-search-header {
      display: flex;
      align-items: center;
      margin-bottom: 1.5rem;
    }

    .user-search-header i {
      font-size: 1.8rem;
      margin-right: 0.75rem;
    }

    .user-search-header h2 {
      font-size: 1.5rem;
      font-weight: 600;
    }

    .user-search-content {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .user-search-input {
      flex: 1;
      min-width: 300px;
      position: relative;
    }

    .user-search-input input {
      width: 100%;
      padding: 1rem 1rem 1rem 3rem;
      border: none;
      border-radius: 0.5rem;
      font-size: 1.1rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .user-search-input i {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      font-size: 1.2rem;
      color: var(--primary);
    }

    .user-search-btn {
      padding: 1rem 2rem;
      background-color: var(--teal);
      color: white;
      border: none;
      border-radius: 0.5rem;
      font-weight: 600;
      font-size: 1.1rem;
      cursor: pointer;
      transition: all 0.3s;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .user-search-btn:hover {
      background-color: #0c7a6d;
      transform: translateY(-2px);
    }

    /* Table */
    .table-container {
      background-color: var(--white);
      border-radius: 0.75rem;
      margin-bottom: 1.5rem;
      overflow: hidden;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    }

    .table-header {
      padding: 1.25rem 1.5rem;
      border-bottom: 1px solid var(--gray-200);
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: var(--gray-50);
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th, td {
      padding: 1.25rem 1.5rem;
      text-align: left;
      border-bottom: 1px solid var(--gray-200);
    }

    th {
      background-color: var(--gray-50);
      font-weight: 600;
      color: var(--gray-700);
      font-size: 0.875rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    tr:hover {
      background-color: var(--gray-50);
    }

    /* Status Badge */
    .status-badge {
      display: inline-block;
      padding: 0.4rem 0.8rem;
      border-radius: 50px;
      font-size: 0.85rem;
      font-weight: 500;
    }

    .status-active {
      background-color: rgba(37, 99, 235, 0.1);
      color: var(--primary);
    }

    .status-inactive {
      background-color: rgba(220, 38, 38, 0.1);
      color: var(--danger);
    }

    /* Actions */
    .action-link {
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      font-weight: 500;
      display: inline-flex;
      align-items: center;
      margin-right: 0.5rem;
      font-size: 0.875rem;
      transition: all 0.2s;
      text-decoration: none;
    }

    .action-link:hover {
      transform: translateY(-2px);
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .action-link i {
      margin-right: 0.5rem;
    }

    .view-link {
      background-color: var(--primary);
      color: white;
    }

    .delete-link {
      background-color: var(--danger);
      color: white;
    }

    /* Medicine Report */
    .medicine-report {
      background-color: var(--white);
      border-radius: 0.75rem;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    }

    .medicine-report h3 {
      margin-bottom: 1rem;
      color: var(--primary);
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .report-stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
      margin-bottom: 1.5rem;
    }

    .report-table {
      margin-top: 1.5rem;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .table-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.75rem;
      }
      
      .table-wrapper {
        overflow-x: auto;
      }
      
      .action-link {
        margin-bottom: 0.5rem;
      }
      
      .search-form {
        flex-direction: column;
      }
      
      .search-group {
        min-width: 100%;
      }
    }
    
    /* Footer */
    footer {
      background-color: var(--white);
      color: var(--gray-600);
      padding: 1.5rem 0;
      text-align: center;
      font-size: 0.875rem;
      margin-top: 2rem;
      border-top: 1px solid var(--gray-200);
    }
    
    /* Empty State */
    .empty-state {
      text-align: center;
      padding: 3rem;
    }
    
    .empty-state i {
      font-size: 3rem;
      color: var(--gray-300);
      margin-bottom: 1rem;
    }
    
    .empty-state h3 {
      font-size: 1.25rem;
      color: var(--gray-700);
      margin-bottom: 0.5rem;
    }
    
    .empty-state p {
      color: var(--gray-500);
    }
  </style>
</head>
<body>
  <div class="min-h-screen">
    <!-- Header -->
    <header>
      <div class="container header-content">
        <h1 class="header-title">
          <i class="fas fa-users-cog"></i> Usimamizi wa Watumiaji
        </h1>
      </div>
    </header>

    <!-- Main Content -->
    <main class="container">
      <!-- Stats Cards -->
      <div class="stats-grid">
        <div class="stat-card">
          <i class="fas fa-users stat-icon"></i>
          <p>Watumiaji Wote</p>
          <h3><%= formatCount(stats.totalUsers) %></h3>
          <div class="stat-meta">Kliniki zote: <%= clinics.length %></div>
        </div>
        <div class="stat-card">
          <i class="fas fa-user-check stat-icon"></i>
          <p>Watumiaji Waliotumia</p>
          <h3><%= formatCount(stats.activeUsers) %></h3>
          <div class="stat-meta"><%= ((stats.activeUsers / stats.totalUsers) * 100 || 0).toFixed(1) %>% ya watumiaji</div>
        </div>
        <div class="stat-card">
          <i class="fas fa-user-times stat-icon"></i>
          <p>Watumiaji Hawajatumia</p>
          <h3><%= formatCount(stats.inactiveUsers) %></h3>
          <div class="stat-meta"><%= ((stats.inactiveUsers / stats.totalUsers) * 100 || 0).toFixed(1) %>% ya watumiaji</div>
        </div>
        <div class="stat-card">
          <i class="fas fa-pills stat-icon"></i>
          <p>Jumla Matumizi</p>
          <h3><%= formatCount(stats.summary.totalConsumption) %></h3>
          <div class="stat-meta">Wastani: <%= formatCount(stats.summary.averageUsagePerUser.toFixed(2)) %> kwa mtumiaji</div>
        </div>
      </div>

      <!-- User Search Card -->
      <div class="user-search-card">
        <div class="user-search-header">
          <i class="fas fa-search"></i>
          <h2>Tafuta Mtumiaji</h2>
        </div>
        <div class="user-search-content">
          <form method="GET" action="/admin/watumiaji" class="user-search-input">
            <i class="fas fa-user"></i>
            <input 
              type="text" 
              name="username" 
              value="<%= filters.username || '' %>"
              placeholder="Andika jina la mtumiaji..." 
            >
          </form>
          <button class="user-search-btn" type="submit" form="filters-form">
            <i class="fas fa-search"></i> Tafuta Mtumiaji
          </button>
        </div>
      </div>

      <!-- Selected Medicine Report -->
      <% if (stats.selectedMedicine) { %>
        <div class="medicine-report">
          <h3><i class="fas fa-pills"></i> Ripoti ya <%= stats.selectedMedicine.name %></h3>
          
          <div class="report-stats">
            <div class="stat-card">
              <p>Jumla Iliumika</p>
              <h3><%= formatCount(stats.selectedMedicine.totalUsed) %></h3>
            </div>
            <div class="stat-card">
              <p>Kilibaki</p>
              <h3><%= formatCount(stats.selectedMedicine.remaining) %></h3>
            </div>
            <div class="stat-card">
              <p>Watumiaji</p>
              <h3><%= formatCount(stats.selectedMedicine.userCount) %></h3>
            </div>
            <div class="stat-card">
              <p>Vituo</p>
              <h3><%= formatCount(stats.selectedMedicine.clinicCount) %></h3>
            </div>
          </div>

          <h4 style="margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.5rem;">
            <i class="fas fa-users"></i> Watumiaji walioitumia:
          </h4>
          <div class="table-wrapper">
            <table class="report-table">
              <thead>
                <tr>
                  <th>Jina</th>
                  <th>Kliniki</th>
                  <th>Kiasi</th>
                  <th>Tarehe ya Mwisho</th>
                </tr>
              </thead>
              <tbody>
                <% stats.selectedMedicine.users.forEach(user => { %>
                  <tr>
                    <td><%= user.name %></td>
                    <td><%= user.clinic %></td>
                    <td><%= formatCount(user.amountUsed) %></td>
                    <td><%= user.lastUsed %></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        </div>
      <% } %>

      <!-- Clinic Summary -->
      <div class="table-container">
        <div class="table-header">
          <h2>Muhtasari wa Matumizi kwa Kliniki</h2>
        </div>
        <div class="table-wrapper">
          <table>
            <thead>
              <tr>
                <th>Kliniki</th>
                <th>Watumiaji</th>
                <th>Waliotumia</th>
                <th>Hawajatumia</th>
                <th>Jumla Matumizi</th>
                <th>Dawa Inayotumika Zaidi</th>
              </tr>
            </thead>
            <tbody>
              <% stats.summary.clinicsSummary.forEach(clinic => { %>
                <tr>
                  <td><%= clinic.name %></td>
                  <td><%= formatCount(clinic.totalUsers) %></td>
                  <td><%= formatCount(clinic.activeUsers) %></td>
                  <td><%= formatCount(clinic.inactiveUsers) %></td>
                  <td><%= formatCount(clinic.totalConsumption) %></td>
                  <td>
                    <% if (clinic.mostUsedMedicine) { %>
                      <%= clinic.mostUsedMedicine.name %> (<%= formatCount(clinic.mostUsedMedicine.amount) %>)
                    <% } else { %>
                      Hakuna data
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Most Consumed Medicines -->
      <div class="table-container">
        <div class="table-header">
          <h2>Dawa Zinazotumika Zaidi</h2>
        </div>
        <div class="table-wrapper">
          <table>
            <thead>
              <tr>
                <th>Dawa</th>
                <th>Aina</th>
                <th>Jumla Matumizi</th>
                <th>Kilibaki</th>
                <th>Watumiaji</th>
                <th>Vituo</th>
              </tr>
            </thead>
            <tbody>
              <% stats.mostConsumedMedicines.forEach(medicine => { %>
                <tr>
                  <td><%= medicine.name %></td>
                  <td><%= medicine.aina %></td>
                  <td><%= formatCount(medicine.totalUsed) %></td>
                  <td><%= formatCount(medicine.remaining) %></td>
                  <td><%= formatCount(medicine.userCount) %></td>
                  <td><%= formatCount(medicine.clinicCount) %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Filters -->
      <div class="search-section">
        <div class="search-header">
          <i class="fas fa-filter"></i>
          <h2>Chuja Matokeo</h2>
        </div>
        
        <form id="filters-form" method="get" action="/admin/watumiaji">
          <div class="search-form">
            <div class="search-group">
              <label class="search-label">Kliniki:</label>
              <select class="search-select" name="clinicId">
                <option value="">Wote</option>
                <% clinics.forEach(clinic => { %>
                  <option value="<%= clinic.id %>" <%= filters.clinicId === clinic.id ? 'selected' : '' %>>
                    <%= clinic.jina %>
                  </option>
                <% }) %>
              </select>
            </div>
            
            <div class="search-group">
              <label class="search-label">Aina ya Dawa:</label>
              <select class="search-select" name="dawaType">
                <option value="">Wote</option>
                <option value="antibiotic" <%= filters.dawaType === 'antibiotic' ? 'selected' : '' %>>Antibiotic</option>
                <option value="antiviral" <%= filters.dawaType === 'antiviral' ? 'selected' : '' %>>Antiviral</option>
                <option value="analgesic" <%= filters.dawaType === 'analgesic' ? 'selected' : '' %>>Analgesic</option>
              </select>
            </div>
            
            <div class="search-group">
              <label class="search-label">Dawa maalum:</label>
              <select class="search-select" name="selectedMedicine">
                <option value="">Chagua dawa...</option>
                <% stats.allMedicines.forEach(medicine => { %>
                  <option value="<%= medicine.id %>" <%= filters.selectedMedicine === medicine.id ? 'selected' : '' %>>
                    <%= medicine.jina %> (<%= medicine.aina %>)
                  </option>
                <% }) %>
              </select>
            </div>
          </div>
          
          <div class="search-actions">
            <button type="submit" class="btn btn-primary">
              <i class="fas fa-filter"></i> Chuja
            </button>
            <a href="/admin/watumiaji" class="btn btn-secondary">
              <i class="fas fa-times"></i> Safisha
            </a>
          </div>
        </form>
      </div>

      <!-- Users Table -->
      <div class="table-container">
        <div class="table-header">
          <h2>Orodha ya Watumiaji (<%= watumiaji.length %>/<%= stats.totalUsers %>)</h2>
          <div>
            <span class="status-badge status-active"><i class="fas fa-check-circle"></i> <%= stats.activeUsers %> Active</span>
            <span class="status-badge status-inactive"><i class="fas fa-times-circle"></i> <%= stats.inactiveUsers %> Inactive</span>
          </div>
        </div>
        
        <div class="table-wrapper">
          <table>
            <thead>
              <tr>
                <th>Jina</th>
                <th>Kliniki</th>
                <th>Matumizi ya Mwisho</th>
                <th>Jumla Matumizi</th>
                <th>Dawa Zilizotumika</th>
                <th>Status</th>
                <th>Vitendo</th>
              </tr>
            </thead>
            <tbody>
              <% if (watumiaji.length === 0) { %>
                <tr>
                  <td colspan="7" class="empty-state">
                    <i class="fas fa-user-slash"></i>
                    <h3>Hakuna Watumiaji Waliochujwa</h3>
                    <p>Badilisha chaguo la utafutaji au uondoe baadhi ya vichujio</p>
                  </td>
                </tr>
              <% } else { %>
                <% watumiaji.forEach(user => { %>
                  <tr>
                    <td>
                      <div style="font-weight: 600; color: var(--gray-800);"><%= user.jina %></div>
                      <% if (user.maelezo) { %>
                        <div style="font-size: 0.875rem; color: var(--gray-500); margin-top: 0.25rem;">
                          <i class="fas fa-info-circle"></i> <%= user.maelezo %>
                        </div>
                      <% } %>
                    </td>
                    <td><%= user.clinic %></td>
                    <td><%= user.lastUsage %></td>
                    <td><%= formatCount(user.totalUsed) %></td>
                    <td><%= user.medicinesUsed %></td>
                    <td>
                      <span class="status-badge <%= user.status === 'Active' ? 'status-active' : 'status-inactive' %>">
                        <i class="fas <%= user.status === 'Active' ? 'fa-check-circle' : 'fa-times-circle' %>"></i> <%= user.status %>
                      </span>
                    </td>
                    <td>
                      <a href="/mtumiaji/angalia/<%= user.id %>" class="action-link view-link">
                        <i class="fas fa-eye"></i> Angalia
                      </a>
                      <a href="/mtumiaji/futa/<%= user.id %>" class="action-link delete-link"
                         onclick="return confirm('Una uhakika unataka kufuta mtumiaji huyu?')">
                        <i class="fas fa-trash-alt"></i> Futa
                      </a>
                    </td>
                  </tr>
                <% }) %>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>

      <a href="/" class="btn btn-secondary" style="margin-bottom: 2rem; display: inline-flex; align-items: center;">
        <i class="fas fa-arrow-left"></i> Rudi kwenye dashibodi
      </a>
    </main>
    
    <footer>
      <div class="container">
        <p>&copy; <%= new Date().getFullYear() %> Mfumo wa Udhibiti wa Dawa. Haki zote zimehifadhiwa.</p>
      </div>
    </footer>
  </div>
  
  <script>
    // Add focus to user search input
    document.querySelector('.user-search-input input').focus();
    
    // Submit form when user presses enter in search field
    document.querySelector('.user-search-input input').addEventListener('keypress', function(e) {
      if (e.key === 'Enter') {
        document.getElementById('filters-form').submit();
      }
    });
  </script>
</body>
</html>
